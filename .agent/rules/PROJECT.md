---
trigger: always_on
---

- The project agentic Architecture is described in `docs`.
- Always update docs if changed or updated the architecture.
- Tests are defined in `tests` folder to verify AI capabilities.