# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║              Cowork CLI — Environment Configuration                         ║
# ║  Copy this file to .env and fill in your API keys.                          ║
# ║  Never commit your .env file to version control.                            ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# ─── Core LLM Provider ────────────────────────────────────────────────────────
# Required: The AI backbone for all reasoning, routing, and compression.

# OpenAI (default)
# Sign up: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: Override the API endpoint (for OpenAI-compatible providers)
# Examples: https://api.openai.com/v1 | https://api.groq.com/openai/v1
#           https://openrouter.ai/api/v1 | http://localhost:11434/v1 (Ollama)
COWORK_API_ENDPOINT=https://api.openai.com/v1

# Optional: Override the default model
# Examples: gpt-4o | gpt-4o-mini | claude-3-5-sonnet | llama3-70b-8192
COWORK_MODEL=gpt-4o-mini


# ─── YOUTUBE_TOOLS ────────────────────────────────────────────────────────────
# Tools: youtube_search, youtube_metadata
# Sign up: https://console.cloud.google.com → Enable "YouTube Data API v3"
# Free tier: 10,000 units/day (search = 100 units, metadata = 1 unit)
YOUTUBE_API_KEY=


# ─── SEARCH_TOOLS ─────────────────────────────────────────────────────────────

# Google Custom Search JSON API (PREFERRED — native Google results, no proxy)
# Tools: google_cse_search, google_search (primary backend)
# Step 1 — Get API key: https://console.cloud.google.com → Enable "Custom Search API"
# Step 2 — Create Search Engine: https://programmablesearchengine.google.com/
#           Set it to search the entire web (toggle "Search the entire web")
# Free tier: 100 queries/day | Paid: $5 per 1,000 queries
GOOGLE_API_KEY=
GOOGLE_SEARCH_ENGINE_ID=

# Google Search via SerpAPI (FALLBACK — used if Google CSE keys are absent)
# Tools: google_search (fallback backend)
# Sign up: https://serpapi.com/
# Free tier: 100 searches/month | Paid: from $50/month
SERPAPI_KEY=

# Brave Search API
# Tools: brave_search
# Sign up: https://api.search.brave.com/
# Free tier: 2,000 queries/month | Paid: from $3/1000 queries
BRAVE_SEARCH_API_KEY=


# ─── WEB_TOOLS ────────────────────────────────────────────────────────────────
# Tools: firecrawl_scrape, firecrawl_crawl
# Sign up: https://firecrawl.dev/
# Free tier: 500 credits/month | Paid: from $16/month
# Best for: JS-rendered pages, documentation sites, complex layouts
FIRECRAWL_API_KEY=


# ─── NEWS_TOOLS ───────────────────────────────────────────────────────────────
# Tools: newsapi_headlines
# Sign up: https://newsapi.org/register
# Free tier: 100 requests/day (developer plan, no commercial use)
# Paid: from $449/month (business plan)
NEWSAPI_KEY=


# ─── WEATHER_TOOLS ────────────────────────────────────────────────────────────
# Tools: openweather_current, openweather_forecast
# Sign up: https://openweathermap.org/api
# Free tier: 1,000 calls/day (current weather + 5-day forecast included)
# Paid: from $40/month for higher limits
OPENWEATHER_API_KEY=


# ─── MEDIA_TOOLS ──────────────────────────────────────────────────────────────
# Tools: tmdb_search, tmdb_details
# Sign up: https://www.themoviedb.org/settings/api
# Free: Completely free for non-commercial use (rate limited to ~40 req/10s)
TMDB_API_KEY=


# ─── CODING_TOOLS ───────────────────────────────────────────────────────────────
# Tools: github_search
# Sign up: https://github.com/settings/tokens
# Optional: Without a token, GitHub API allows 10 req/min (unauthenticated)
#           With a token: 5,000 req/hour
GITHUB_TOKEN=


# ─── KNOWLEDGE_TOOLS ──────────────────────────────────────────────────────────
# Tools: wikipedia_search, wikipedia_article
# No API key required — Wikipedia's API is free and open.
# (No variable needed here — these tools always work)


# ─── COMMUNICATION_TOOLS ──────────────────────────────────────────────────────
# Tools: smtp_send_email, telegram_send_message, slack_send_message, twitter_post_tweet
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Telegram: Get from @BotFather
TELEGRAM_BOT_TOKEN=
# Slack: Create an app at api.slack.com/apps → "chat:write" scope
SLACK_BOT_TOKEN=xoxb-...
# Twitter (X): API v2 Bearer Token
TWITTER_BEARER_TOKEN=

# ─── GOOGLE_TOOLS ─────────────────────────────────────────────────────────────
# Tools: google_calendar (list/create), google_drive (search/upload/read), gmail_send_email
# Setup: Requires ~/.cowork/google_credentials.json (OAuth2 Desktop app)
# (Scope: Full Calendar + Drive File access)

# ─── Notes ────────────────────────────────────────────────────────────────────
# • Leave a variable empty (KEY=) to disable that tool group.
# • The AI will gracefully inform you when a key is missing.
# • Tools without keys (wikipedia_*, youtube_transcript, github_search)
#   always work regardless of configuration.
# • Add new tools by creating a function in cowork/tools_external.py,
#   registering it in EXTERNAL_TOOLS, EXTERNAL_TOOL_HANDLERS, and KEY_REQUIREMENTS,
#   then adding the env var here.


# ─── MULTIMODAL_TOOLS ─────────────────────────────────────────────────────────
# Tools: vision_analyze, image_generate, speech_to_text, text_to_speech
# All four services use separate endpoint + token + model so you can mix providers.
# Configure at runtime: cowork mm set <service> endpoint|token|model <value>
# Or pre-configure via .env (recommended for token security):

# Vision / Image Analysis — POST /v1/recognize
# Compatible: OpenAI gpt-4o, LLaVA, any OpenAI-vision-compatible endpoint
mm_vision_endpoint=https://api.openai.com/v1
mm_vision_token=sk-...
mm_vision_model=gpt-4o

# Image Generation — POST /v1/images/generations
# Compatible: OpenAI DALL-E, Stability AI, Together AI, any DALL-E-compatible endpoint
mm_image_endpoint=https://api.openai.com/v1
mm_image_token=sk-...
mm_image_model=dall-e-3

# Speech-to-Text (ASR / Whisper) — POST /v1/audio/transcriptions
# Compatible: OpenAI Whisper, FasterWhisper, local Whisper server
mm_asr_endpoint=https://api.openai.com/v1
mm_asr_token=sk-...
mm_asr_model=whisper-1

# Text-to-Speech (TTS) — POST /v1/audio/speech
# Compatible: OpenAI TTS, Coqui, Bark, any TTS endpoint returning audio bytes
mm_tts_endpoint=https://api.openai.com/v1
mm_tts_token=sk-...
mm_tts_model=tts-1
mm_tts_voice=